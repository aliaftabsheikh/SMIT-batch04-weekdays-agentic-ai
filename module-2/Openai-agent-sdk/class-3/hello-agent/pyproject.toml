[project]
name = "hello-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "aliaftabsheikh", email = "aliaftabsheikh1@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "openai-agents>=0.20.0",
    "python-dotenv>=1.2.2",
]

[project.scripts]
hello-agent = "hello_agent:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
