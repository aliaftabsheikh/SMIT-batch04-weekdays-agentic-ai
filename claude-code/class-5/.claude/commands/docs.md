---
argument-hint: [file-or-path]
description: Write or improve documentation for the given code
---

Document $ARGUMENTS (if empty, document the files in the current diff).

Use the `docs-writer` subagent. Write for the right audience, use concrete
runnable examples, match the project's existing doc style, and keep it accurate
to the code.
