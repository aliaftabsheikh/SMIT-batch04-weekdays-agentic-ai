import type { Metadata } from "next";
import { Space_Grotesk, Inter } from "next/font/google";
import "./globals.css";
import { Navbar } from "@/components/Navbar";
import { Footer } from "@/components/Footer";

const spaceGrotesk = Space_Grotesk({
  variable: "--font-space-grotesk",
  subsets: ["latin"],
  weight: ["400", "500", "600", "700"],
});

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Nexus — The Decentralized Liquidity Layer",
  description:
    "Nexus is the trustless liquidity protocol powering the next generation of on-chain finance. Swap, stake, and bridge across chains with zero compromise.",
  keywords: ["web3", "defi", "crypto", "liquidity", "blockchain", "nexus"],
  openGraph: {
    title: "Nexus — The Decentralized Liquidity Layer",
    description:
      "Trustless, cross-chain liquidity for the next generation of on-chain finance.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${spaceGrotesk.variable} ${inter.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-background text-foreground">
        <Navbar />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
